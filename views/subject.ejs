<%- include('partial/navbar',{status:''}); %>
      <!-- body table -->
      <div>
        <h1>Subject Information:</h1>
    </div>
    <table class="table table-sm table-dark">
        <tr>
            <th scope="col">ID</th>
            <th scope="col">Subject</th>
        </tr>
        <% for( i = 0 ; i < data.length ; i++ ){ %>
            <tr>
                <th scope="col"><%= data[i].id %></th>
                <th scope="col"><%= data[i].subject %></th> 
            </tr>
        <%  }   %>
    </table>
        <style>
            div{
                text-align: center;
            }
        </style>
</body>
</html>